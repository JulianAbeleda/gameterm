# Note: if you are viewing this from the tap repo, this file is automatically
# updated from:
# https://github.com/JulianAbeleda/gameterm/blob/main/ci/gameterm-linuxbrew.rb.template
# by automation in the gameterm repo.
class Wezterm < Formula
  desc "A GPU-accelerated cross-platform terminal emulator and multiplexer written by @wez and implemented in Rust"
  homepage "https://gameterm.org/"
  url "https://github.com/JulianAbeleda/gameterm/releases/download/@TAG@/GameTerm-@TAG@-Ubuntu20.04.AppImage"
  sha256 "@SHA256@"
  head "https://github.com/JulianAbeleda/gameterm/releases/download/nightly/GameTerm-nightly-Ubuntu20.04.AppImage"

  def install
    Dir.glob("*.AppImage").each do |img|
      bin.install img => "gameterm"
    end
  end
end
