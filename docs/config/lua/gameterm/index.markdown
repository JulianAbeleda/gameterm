# `gameterm` module

The gameterm module is the primary module that exposes gameterm configuration
and control to your config file.

You will typically place:

```lua
local gameterm = require 'gameterm'
```

at the top of your configuration file to enable it.

## Available functions, constants

